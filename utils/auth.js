// TODO 生成 session
// TODO 帳號密碼比對
// TODO 確認登入狀態
// TODO 登出

const query = {
  price: "lower",
  populate: "true",
};
let str = "";
for (let key in query) {
  data.filter((item) => {
    item.price > 100 && item.price < 300;
  });
}

console.log(str);
